import styled from "styled-components";
import {
  space,
  width,
  fontSize,
  textColor,
  bgColor,
  color,
  fontFamily,
  textAlign,
  lineHeight,
  fontWeight,
  fontStyle,
  letterSpacing
} from "styled-system";

const Text = styled.div`
  ${space}
  ${width}
  ${fontSize}
  ${textColor}
  ${bgColor}
  ${color}
  ${fontFamily}
  ${textAlign}
  ${lineHeight}
  ${fontWeight}
  ${fontStyle}
  ${letterSpacing}
`;

export default Text;
